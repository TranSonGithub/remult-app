export enum Size {
  S = 'S',
  M = 'M',
  L = 'L',
}

export enum TypeMenu {
  DRINK = 'DRINK',
  MAIN = 'MAIN',
}

export enum StatusOrder {
  PEDDING = 'PEDDING',
  CANCEL = 'CANCEL',
  READY = 'READY',
  SUCCESS = 'SUCCESS',
}

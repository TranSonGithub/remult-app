export enum Size {
  S = 'S',
  M = 'M',
  L = 'L',
}

export enum TypeMenu {
  DRINK = 'DRINK',
  MAIN = 'MAIN',
}

export enum StatusOrder {
  PENDING = 'PENDING',
  CANCEL = 'CANCEL',
  TRANSFERRING = 'TRANSFERRING',
  READY = 'READY',
  SUCCESS = 'SUCCESS',
}
